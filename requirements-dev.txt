# Developer / test dependencies. The runtime package needs only
# requirements.txt; install this set to run tests/ standalone.
-r requirements.txt

pytest

# The dynamics-facing tests (test_planar_translation_decomposition,
# test_joint_dynamics_value_path, test_helical_joint, test_skew_axis) validate
# the parse against Pinocchio and against the sibling RBDReference package
# (checked out NEXT TO this repo, directory named exactly `RBDReference`, the
# common parent on sys.path — tests/conftest.py handles the path).
#
# Version pins mirror RBDReference/requirements-dev.txt and are LOAD-BEARING:
# pin 4.x drops pinocchio.pc + restructures headers, and pin 3.9's pywrap
# links liburdfdom_*.so.4 (pairing it with urdfdom 6.x breaks
# `import pinocchio` at load).
pin<4
cmeel-eigen
cmeel-urdfdom<5
